
## Local Documentation Mirror

OpenClaw docs are mirrored locally at `~/.openclaw/docs/` (start at `INDEX.md`), refreshed nightly. Consult the local mirror before searching the web for OpenClaw questions. The mirror content is NOT version-controlled (regenerated by `scripts/mirror_openclaw_docs.py`); only the script is.
